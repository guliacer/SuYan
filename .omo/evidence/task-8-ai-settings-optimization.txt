TASK 8 EVIDENCE — AI 连接区 UI 重构
=======================================
Worker: continuation from session ses_fed6b2decffee7Ip4XxzmUzTX1
Baseline commit: 34eca01 (feat(ai): add ai settings backup export/import service)
Date: 2026-08-18

Scope (plan T8): 重排 AiSettingsDialog.tsx 连接区（约 891-1414 行）：
- 左供应商侧栏卡片紧凑化（去 min-h-14，改紧凑单行 + 选中态）
- Base URL / API Key 两列并排（原纵向堆叠改 grid 两列）
- 当前供应商 Header 紧凑化
- 模型列表保持紧凑表格
必须：保留添加/删除/复制 key/测试/默认标记全部交互；数据流不变。

=== CHANGES (git diff) ===
1. 供应商侧栏列表项（profile 卡片）：
   Before: `grid min-h-14 gap-1 rounded-lg border px-2.5 py-2.5`（两行式：名称行 + 状态行）
   After:  `flex items-center gap-2 rounded-md border px-2.5 py-1.5`（紧凑单行）
   内容合并为单行：GripVertical + 名称 + 状态圆点 + 模型/未配置密钥 + 默认标记(Star)
   → 每项从两行压缩为单行，卡片高度显著下降。
2. 侧栏头部 "OpenAI 兼容 + 计数" 从带边框卡片改为轻量标题行
   （去外层 border/bg-panel 包裹，改 flex justify-between 单行）。
3. 当前连接 Header 紧凑化：
   - 外层 `gap-3 px-6 py-5` → `gap-2 px-6 py-4`
   - "当前连接" 标题 `text-sm font-semibold text-foreground` → `text-xs font-medium text-muted`
4. Base URL / API Key 两列并排：
   - 凭据区外层 `<div className="grid gap-4">` → `grid gap-4 min-[820px]:grid-cols-2 min-[820px]:items-start`
   - 在 >= 820px 视口，接口地址与 API Key 并排两列；窄屏回退纵向堆叠。
5. 模型列表：已符合计划"紧凑表格"要求（grid-cols-[模型/能力/状态] 行内布局），
   未改动（无需再动，避免过度重构）。

=== PRESERVED (计划约束核验) ===
- 添加供应商 <Plus> 按钮、选中态 data-ai-profile-active、拖拽排序（draggable/reorderProfiles/GripVertical）全部保留
- 删除/复制 key/测试/默认标记（Star "设为默认"）全部保留
- 数据流不变：selectedProfileId/activeProfileId/drafts/patchProfile/reorderProfiles 均未改动
- 未改任何 handler / state 逻辑，纯 className 布局调整

=== ACCEPTANCE ===
[PASS] npx tsc --noEmit → 0 错误（renderer）
[PASS] npx vitest run tests/unit/aiSettingsDialogControls.test.ts → 3/3 绿
[PASS] npx vitest run（全量）→ 129 文件 / 943 测试 全绿
[PASS] 所有交互按钮 aria-label 与 onClick 未动（源码 diff 确认）

Playwright 截图 QA：本环境无桌面显示，无法运行 Electron 可视化。
按 T7 先例，以源码结构断言代替视觉 QA，视觉验收延至 T14 打包后手测。

=== COMMIT ===
refactor(ai): restructure connection settings layout
文件：src/features/library/components/AiSettingsDialog.tsx
     .omo/evidence/task-8-ai-settings-optimization.txt