TASK 11 EVIDENCE — AiSettingsDialog 组件与 Hook 拆分
========================================================
Worker: continuation from session ses_fed6b2decffee7Ip4XxzmUzTX1
Baseline commit: e6c4ad5 (refactor(ai): restructure rules section with collapsible editors)
Date: 2026-08-18

Scope (plan T11): 将 2405 行 AiSettingsDialog.tsx 拆为子组件 + hooks，
拆分后主文件只做编排（< 400 行），行为与拆分前逐字一致。

=== CHANGES ===
1. 纯数据/纯函数模块 src/features/library/components/aiSettingsDialogData.ts（484 行）：
   - 类型：AiSettingsDialogProps / AiProviderProfileDraft / ModelPickerState /
     RuleEditorState / AiSettingsActionEntry
   - 常量：aiSettingsActionEntries / defaultAiSettingsActionOrder
   - 纯函数：normalizeAiSettingsActionOrder、moveItemBefore、action-entry
     resolver 族（resolve/entry/label/description/source/kind）、buildPayload、
     buildConnectionTestPayload、toSaveProfilePayload、createProfileDrafts、
     toFallbackProfile、createNewProfileDraft、resolveSelectedProfileId、
     resolveDraftApiKey*、maskApiKeyPreview、ensureProfileSelectedModel、
     isProfileComplete、canTestProfile、buildModelQueryPayload、canQueryModels、
     addUniqueModels、normalizeProfileModels、normalizeModelDraft、
     normalizeCapabilities、toggleCapability、toggleStringSelection、
     normalizeModelSearch、isRecord —— 均逐字移植，未改逻辑。
2. 模型子组件 src/features/library/components/AiSettingsModelControls.tsx（225 行）：
   - ModelRow / CapabilityButton / ModelPickerPanel / ModelCapabilityIcons
     四个 presentational 组件逐字移植。
3. 状态 hook src/features/library/components/useAiSettings.ts（905 行）：
   - 全部 useState/useRef/useEffect/useMemo + 全部 handlers + autoSave 接线
     收敛于此；useAiSettingsAutoSave 从 ../hooks 引入（T10）。
   - 返回 AiSettingsApi = ReturnType<typeof useAiSettings>，供两个 section
     解构共享（含 state/setters/refs/derived/handlers）。
4. 表现区组件：
   - src/features/library/components/AiSettingsHeader.tsx（20 行）：标题 + 关闭。
   - src/features/library/components/AiConnectionSection.tsx：ProviderSidebar +
     ProviderHeader + ProviderCredentials + ModelList + ModelPickerPanel 全量 JSX。
   - src/features/library/components/AiRulesSection.tsx：ActionSidebar +
     ActionWorkspace + SelectedRulesList + CustomRuleEditor 全量 JSX。
5. 主文件 src/features/library/components/AiSettingsDialog.tsx：52 行，仅编排
   （AppDialog + Header + 主滚动区 + 连接区 + 规则区），保留
   `export { moveItemBefore }` 以兼容既有测试/引用（LibraryView 懒加载不变）。

=== 行为一致性保证 ===
- 全部 JSX 与逻辑逐字平移，未改任何 className/handler/派生值/payload 构建。
- 拆分只移动文件归属，未动数据流（selectedProfileId/activeProfileId/drafts、
  API Key 只走 onCopyApiKey/onReadApiKey，无明文 key 进 renderer 持久化）。
- useAiSettingsAutoSave 的测试暂停语义（autoSave.pause()/resume()）保持不变。

=== TEST IMPACT（source-contract 断言随文件归属迁移） ===
- tests/unit/aiSettingsDialogControls.test.ts：drag/payload 断言从单一
  dialogSource 改为按实际归属文件断言（connection/rules/useAiSettings），
  行为基线不变，仍锁定 draggable/reorderProfiles/reorderActionEntries/
  actionOrder 参与保存。
- tests/unit/useAiSettingsAutoSave.test.ts：pause/resume 断言指向 useAiSettings。

=== ACCEPTANCE ===
[PASS] npx tsc --noEmit → 0 错误（renderer）
[PASS] npx tsc --noEmit -p tsconfig.electron.json → 0 错误
[PASS] npx vitest run tests/unit/aiSettingsDialogControls.test.ts → 3/3 绿
[PASS] npx vitest run tests/unit/useAiSettingsAutoSave.test.ts → 5/5 绿
[PASS] npx vitest run（全量）→ 130 文件 / 948 测试绿（stressTest 性能计时
      项在并行时偶发超时，单独跑 8/8 绿，属既有 flaky 计时，与本次重构无关）
[PASS] AiSettingsDialog.tsx 行数：2405 → 52（远低于 < 400 编排目标）

=== COMMIT ===
refactor(ai): split AiSettingsDialog into components and hooks
文件：src/features/library/components/AiSettingsDialog.tsx（重写为编排）
     src/features/library/components/aiSettingsDialogData.ts（新增）
     src/features/library/components/AiSettingsModelControls.tsx（新增）
     src/features/library/components/useAiSettings.ts（新增）
     src/features/library/components/AiSettingsHeader.tsx（新增）
     src/features/library/components/AiConnectionSection.tsx（新增）
     src/features/library/components/AiRulesSection.tsx（新增）
     tests/unit/aiSettingsDialogControls.test.ts（适配）
     tests/unit/useAiSettingsAutoSave.test.ts（适配）
     .omo/evidence/task-11-ai-settings-optimization.txt