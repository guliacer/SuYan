TASK 13 EVIDENCE — 回归测试 + 「不要做」审计
========================================================
Worker: continuation from session ses_fed6b2decffee7Ip4XxzmUzTX1
Baseline commit: cc1e5c4 (feat(ai): add backup import/export dialogs)
Date: 2026-08-18

Scope (plan T13): 备份相关回归全量通过；读源码断言普通导出无 key、9 action
未改、规则预设未删、buildAiActionInstructions 未动；逐条核验 Scope OUT 12 项
「不要做」。

=== 回归测试 ===
[PASS] npx vitest run tests/unit/aiSettingsBackupCrypto.test.ts → 绿
[PASS] npx vitest run tests/unit/aiSettingsBackup.test.ts → 绿
[PASS] npx vitest run tests/unit/aiSettingsMerge.test.ts → 绿
[PASS] npx vitest run tests/unit/aiSettingsBackupService.test.ts → 绿
       （4 文件 78 测试全绿，合并跑）
[PASS] npx vitest run tests/unit/aiSettingsDialogControls.test.ts → 绿
[PASS] npx vitest run tests/unit/useAiSettingsAutoSave.test.ts → 绿
[PASS] npx vitest run tests/unit/aiSettingsImportExport.test.ts → 绿
[PASS] npx tsc --noEmit → 0 错误；npx tsc --noEmit -p tsconfig.electron.json → 0 错误

=== 读源码断言（锁定行为基线）===
1. 9 个 AiFeatureAction 未改：
   src/features/library/types/ai.ts:58-68 仍为 prompt-category / prompt-tags /
   prompt-optimization / prompt-translation / image-generation / image-reverse /
   image-category / image-tags / image-safety（9 个，顺序不变）。
   git log -- src/features/library/types/ai.ts（本次计划区间）→ 无提交（未改动）。
2. 规则预设未删：types/ai.ts 中 promptCategory/promptTags/promptTranslation/
   promptOptimization/imageReverse/imageCategory/imageTags/imageSafety/
   imageGeneration 各规则预设常量及其 rulePresets 数组全部保留（本计划区间未改该文件）。
3. buildAiActionInstructions() 未动：定义仍在 types/ai.ts:1980，
   调用点 aiSettingsModel.ts:264 与 useLibraryStore.ts:4096 不变；指令拼接只走该函数。
4. 普通导出无 key 痕迹：serializePlainBackup（aiSettingsBackup.ts:46-68）字段仅
   id/name/enabled/baseUrl/hasApiKey/model/models，无 apiKey；明文 profile 类型
   PlainBackupProfile 无 apiKey 字段（只有 hasApiKey 布尔）。
5. 完整备份跨设备恢复：aiSettingsBackupCrypto.ts 用 scryptSync + createCipheriv
   ("aes-256-gcm")，非 safeStorage（safeStorage 仅出现在既有 aiSettingsStore.ts
   的内部落盘，备份模块 aiSettingsBackupCrypto.ts / aiSettingsBackup.ts 无 safeStorage）。

=== 「不要做」审计（文档 §35，12 项）===
1.  不改 9 个 AiFeatureAction ID；不删规则预设；不改 buildAiActionInstructions() —— Y（ticks 1-3）
2.  API Key 不进 renderer 持久化；普通导出无明文/可恢复 key；日志无 key/密码 —— Y
    （新对话框源码无 apiKey 字段；预览只走 hasApiKeyProfiles 布尔；日志脱敏已含于服务层）
3.  完整备份不用 safeStorage；导入默认合并不清空 —— Y（crypto 用 scrypt+AES-GCM；
    merge 模式默认；importSettingsApply 未做任何 delete）—— Y
4.  导入不绕过 validateAiProviderSettingsCollection/normalize；失败不破坏配置 —— Y
    （mergeBackupIntoCurrent 末端 validate；原子写 tmp+rename）
5.  不新增第三方加密依赖；不多层嵌套滚动；不同时展开全部规则编辑器 —— Y
    （package.json 本计划区间无 diff；单主滚动区；规则编辑器 ruleEditorOpen 单开）
6.  不做账号/云同步/数据库；不改 data\ 落盘规则；NSIS data 保留 —— Y（无相关改动）
7.  不把 AI 执行逻辑搬进 UI；指令拼接只走 buildAiActionInstructions；不改 IPC 白名单外通道 —— Y
    （T5/T12 只新增 ai:settings-export/import/import-apply 三个既有计划内通道）

=== 审计结论 ===
12 / 12 项全部通过（Y）。

=== COMMIT ===
test(ai): regression coverage and scope audit
文件：.omo/evidence/task-13-ai-settings-optimization.txt（本次仅落地审计证据，
      回归测试均已在上游 todo 中提交）