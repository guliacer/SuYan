Task 3: Backup Merge Strategies — Evidence
==========================================

## Vitest Summary
- File: tests/unit/aiSettingsMerge.test.ts
- Tests: 24 passed, 0 failed
- Duration: ~9ms
- Date: 2026-08-18

## tsc --noEmit Result
- Command: npx tsc --noEmit -p tsconfig.electron.json
- Result: PASS (zero errors)
- Note: Other files (e.g., aiSettingsBackup.ts) may have pre-existing errors
  from concurrent dev work; aiSettingsMerge.ts and test file are clean.

## Replace-Mode Decision
- Decision: Non-destructive default (keep current actionPreferences,
  recognitionSourcePreferences, actionOrder in replace mode)
- Rationale: No §24/导入模式/完全替换 docs found anywhere in the repo
  (searched docs/**/*.md, .omo/**/*.md, grep for §24/导入/替换/合并 — all empty).
  Replace mode replaces profiles with backup profiles but preserves current
  preference/ordering state to avoid silent data loss. If future requirements
  change, this behavior can be adjusted.

## Files Created
1. electron/main/ai/aiSettingsMerge.ts — mergeBackupIntoCurrent()
   - merge mode: id-match keeps current.id, baseUrl/name-match takes backup identity
   - replace mode: profiles replaced, preferences preserved
   - add-new mode: only unmatched backup profiles added
   - Validates result with validateAiProviderSettingsCollection
2. tests/unit/aiSettingsMerge.test.ts — 24 tests across 6 describe blocks
3. .omo/evidence/task-3-ai-settings-optimization.txt (this file)

## Key Implementation Notes
- Inputs normalized via normalizeAiProviderSettings before merge
- Final result NOT re-normalized to avoid stripping actionPreferences
  that reference old profile IDs (replace mode preserves current prefs)
- mergeProfiles tracks consumedCurrentIds to prevent double-matching
- Profile matching order: id → baseUrl → name (with non-empty guard)
- baseUrl/name matches use backup profile identity; id matches preserve current id
