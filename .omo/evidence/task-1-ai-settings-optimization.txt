Task 1: AI Settings Backup Crypto Module — Evidence
=================================================
Date: 2026-08-18

## 1. Vitest Output (tests/unit/aiSettingsBackupCrypto.test.ts)

RUN  v4.1.9 W:/提示词

 ✓ tests/unit/aiSettingsBackupCrypto.test.ts (14 tests) 716ms

 Test Files  1 passed (1)
      Tests  14 passed (14)
   Duration  1.03s

## 2. TypeScript Compilation (tsconfig.electron.json)

npx tsc --noEmit -p tsconfig.electron.json → exit 0 (no errors)

## 3. Files Created

- electron/main/ai/aiSettingsBackupCrypto.ts (module)
- tests/unit/aiSettingsBackupCrypto.test.ts (tests)

## 4. API Surface

- deriveKey(password: string, salt: Buffer): Buffer — scrypt N=16384,r=8,p=1,keylen=32
- encryptBackup(plaintextUtf8: string, password: string): EncryptedBackupEnvelope
- decryptBackup(envelope, password): string — throws BackupCryptoError(code="AI_SETTINGS_BACKUP_DECRYPT_FAILED") on failure
- type EncryptedBackupEnvelope = { salt: Buffer(16B), iv: Buffer(12B), authTag: Buffer(16B), ciphertext: Buffer }
- class BackupCryptoError extends Error { code: string }

## 5. Test Coverage Summary

- deriveKey: correct length, deterministic for same inputs, different passwords/salts produce different keys
- Round-trip: plaintext → encrypt → decrypt → original plaintext
- UTF-8 Chinese round-trip: Chinese text with punctuation, emoji, newlines, tabs
- Wrong password: throws with code AI_SETTINGS_BACKUP_DECRYPT_FAILED
- Tampered ciphertext: throws with code AI_SETTINGS_BACKUP_DECRYPT_FAILED
- Tampered authTag: throws with code AI_SETTINGS_BACKUP_DECRYPT_FAILED
- Envelope structure: salt=16B, iv=12B, authTag=16B
- Randomness: two encryptions of same text produce different salt/iv/ciphertext

## 6. Git Commit

8491326 feat(ai): add scrypt+AES-256-GCM backup crypto module

## 7. Security Notes

- No third-party dependencies added; uses only node:crypto (scryptSync, createCipheriv, createDecipheriv, randomBytes)
- No safeStorage used (forbidden by plan)
- No passwords or plaintext logged (per .codex/rules/09 R9/R10)
- scrypt params: N=16384, r=8, p=1, keylen=32 (production-safe)
