TASK 5 EVIDENCE — AI Settings Backup IPC Channels + Preload + SuyanApi
======================================================================
Worker: continuation from session ses_fed6b2decffee7Ip4XxzmUzTX1
Baseline commit: 34eca01 (feat(ai): add ai settings backup export/import service)
Date: 2026-08-18 14:15 +08:00

Depends on: T4 (aiSettingsBackupService.ts) ✅ committed 34eca01
Blocks: T12 (import/export dialogs)

=== FILES MODIFIED (4 existing files, no new files) ===
1. electron/shared/ipcChannels.ts
   - IpcChannelName enum: added AiSettingsExport/AiSettingsImport/AiSettingsImportApply
   - ipcChannels map: added aiSettingsExport/aiSettingsImport/aiSettingsImportApply

2. electron/main/ipc/registerIpcHandlers.ts
   - import { exportSettingsBackup, importSettingsApply, importSettingsPreview } from "../ai/aiSettingsBackupService";
   - Registered 3 handlers via existing handleResult pattern, alongside AiSettingsRead/Save (line 450+):
       ipcChannels.aiSettingsExport   → exportSettingsBackup(payload)
       ipcChannels.aiSettingsImport   → importSettingsPreview(payload)
       ipcChannels.aiSettingsImportApply → importSettingsApply(token, mode)

3. electron/preload/index.ts
   - contextBridge exposes: exportAiSettings / importAiSettingsPreview / importAiSettingsApply
   - Each invokes the matching IpcChannelName via the existing `invoke` helper

4. src/types/suyanApi.ts
   - New type AiSettingsImportPreview (summary shape, no keys)
   - SuyanApi interface: added 3 method signatures returning Promise<IpcResult<...>>

=== SYMMETRY CHECK (grep) ===
3 channel names appear in ALL 4 layers (ipcChannels / registerIpcHandlers / preload / suyanApi):
  ai:settings-export        ✅ AiSettingsExport
  ai:settings-import        ✅ AiSettingsImport
  ai:settings-import-apply  ✅ AiSettingsImportApply

=== TYPE CHECKS ===
npx tsc --noEmit -p tsconfig.electron.json  → 0 errors (clean)
npx tsc --noEmit                             → 0 errors (clean)

=== VITEST (full regression) ===
npx vitest run
  Test Files  129 passed (129)
  Tests       943 passed (943)
  Duration    ~61s

=== SECURITY CHECKS ===
- Only whitelisted channels exposed (3 new ones explicitly declared in IpcChannelName enum).
- Return types contain NO plaintext API key:
    exportAiSettings   → { canceled: boolean; filePath?: string }
    importAiSettingsPreview → AiSettingsImportPreview { token, fileName, formatVersion,
      providerCount, newProviderCount, modelCount, hasApiKeyProfiles, actionPreferencesCount,
      errors? }  (hasApiKeyProfiles is boolean, never the key itself)
    importAiSettingsApply → PublicAiProviderSettings (existing public shape with apiKeyPreview
      masked + hasApiKey boolean, never the raw key)
- Errors flow through handleResult → toErrorPayload (existing pattern, line 664), so renderer
  receives { ok:false, error:{code,message} } with the T6 Chinese messages (e.g.
  AI_SETTINGS_BACKUP_DECRYPT_FAILED → "备份密码错误或备份文件已损坏。").
- payload: plain → {type:"plain"}; full → {type:"full", password}. Password only transits
  renderer→main via IPC invoke (never persisted, never logged — T4 service redacts).
- No new third-party deps. No safeStorage. No IPC channel beyond the 3 declared.

=== CONTRACT (consumed by T12) ===
suyanApi.exportAiSettings({ type: "plain" | "full", password? })
  → Promise<IpcResult<{ canceled: boolean; filePath?: string }>>
suyanApi.importAiSettingsPreview({ password? })
  → Promise<IpcResult<AiSettingsImportPreview>>
suyanApi.importAiSettingsApply({ token, mode: "merge" | "replace" | "add-new" })
  → Promise<IpcResult<PublicAiProviderSettings>>

=== NEXT STEPS ===
Commit message: feat(ai): wire ai settings backup IPC channels
Commit ONLY the 4 modified files + this evidence:
  electron/shared/ipcChannels.ts
  electron/main/ipc/registerIpcHandlers.ts
  electron/preload/index.ts
  src/types/suyanApi.ts
  .omo/evidence/task-5-ai-settings-optimization.txt
Never `git add -A` (other untracked dev files belong to other work).
